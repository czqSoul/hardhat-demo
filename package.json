{
  "license": "NONE",
  "scripts": {
    "deploy:node": "npx hardhat node",
    "deploy:localhost": "npx hardhat run --network localhost scripts/deploy.ts",
    "dev": "yarn deploy:localhost",
    "deploy:goerli": "npx hardhat run --network goerli scripts/deploy.ts",
    "goerli": "yarn deploy:goerli"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "dotenv": "^16.0.2",
    "hardhat": "^2.11.1",
    "typescript": "^4.8.2"
  }
}